spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:mysql://localhost:3306/db_project2
    username: root
    password: root123414
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      auto-commit: false

    redis:
      host: localhost
      port: 6379

# ===== 개발용 로깅 =====
logging:
  level:
    com.back: DEBUG
    org.hibernate.orm.jdbc.bind: DEBUG

# ===== OpenAPI 스키마 생성 설정 (개발환경) =====
openapi:
  schema:
    enabled: true
