spring:
  config:
    activate:
      on-profile: dev

  # ===== 개발용 MySQL 설정 =====
  datasource:
    url: jdbc:mysql://localhost:3306/project_management?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true&characterEncoding=UTF-8&useUnicode=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: Park-seungg
    password: 12345
    hikari:
      auto-commit: false

  # ===== 개발용 JPA 설정 =====
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

# ===== 개발용 로깅 =====
logging:
  level:
    com.back: DEBUG
    org.hibernate.orm.jdbc.bind: DEBUG

# ===== OpenAPI 스키마 생성 설정 (개발환경) =====
openapi:
  schema:
    enabled: true
